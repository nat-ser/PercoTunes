<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :morning_music %>
    <%= form.text_field :morning_music %>
  </div>

  <div class="field">
    <%= form.label :afternoon_music %>
    <%= form.text_field :afternoon_music %>
  </div>

  <div class="field">
    <%= form.label :evening_music %>
    <%= form.text_field :evening_music %>
  </div>

  <div class="field">
    <%= form.label :rfid %>
    <%= form.number_field :rfid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
